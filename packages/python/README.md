# Python Package

This package is the Python example used by the repository's GitHub Actions template.

## Commands

```powershell
uv sync --all-extras --dev
uv run ruff check .
uv run pytest
uv build
```

